# 2026-07-19 工作日志

## proposal-review skill 缺陷分析（只分析未修改）

用户要求分析当前 proposal-review skill 是否解决其业务痛点，明确要求**只分析不改项目内容**。

### 用户痛点
原流程：superpowers 生成 specs/plans → 开 4 个终端窗口 → 3 个角色(product-reviewer/system-critic/test-designer)分别审核 → 复制到 review-orchestrator 合并 → superpowers 看结果决定采纳。

期望：1 个终端窗口、说一次要求、3 角色分开审核、输出到 superpowers 文件夹（类似 plans/specs）、命名对齐 superpowers、合并成一个文档、superpowers 自动阅读、标记采纳/不采纳、支持统计分析迭代升级。

### 分析结论

**痛点覆盖度**：9 条诉求中 3 条完全解决、4 条部分解决、2 条未解决。

**P0 缺陷（阻碍核心流程）**：
1. 缺少执行入口——SKILL.md 无 frontmatter、无触发机制、无"单会话如何串起全流程"说明
2. 与 superpowers 双向交接机制完全缺失——输出端/输入端/回写机制都没定义
3. Consolidator 角色定义缺失——protocols/consolidation-protocol.md 有规则，但 roles/ 下无对应角色文件

**P1 缺陷（影响一致性）**：
4. 决策状态枚举三处冲突（SKILL.md / decision-protocol.md / consolidated-review.md 各一套）
5. Consolidated Finding ID 不一致（RV-001 vs CR-001）
6. Test Designer 输出结构与 Finding Protocol 不兼容（AT/AC/BL vs 统一 TD 结构）
7. 角色独立性与"一个终端窗口"根本矛盾未解决（未说明用 subagent 并行）

**P2 缺陷（影响完整性）**：
8. SKILL.md 缺 YAML frontmatter，无法被 skill 系统自动发现
9. 跨轮次统计分析机制完全缺失（只有单轮统计，无纵向追踪）
10. 输入/输出路径命名关联不明确，未对齐 superpowers 日期前缀惯例
11. Phase 5 Proposal Revision 超出 skill 边界（与 Purpose 声明矛盾）
12. review-orchestrator 协议文件缺失

### 核心判断
skill 解决了"审核方法论"问题，但没解决"工程化执行"问题。执行层（入口/并行/串接）、交接层（skill↔superpowers）、统计层（跨轮追踪）三层都留白了。

### 文件清单（已读取分析）
- SKILL.md（主 skill 定义，5 个 Phase）
- roles/product-reviewer.md, system-critic.md, test-designer.md（3 个角色）
- protocols/finding-protocol.md, consolidation-protocol.md, decision-protocol.md（3 个协议）
- templates/product-review.md, system-review.md, test-review.md, consolidated-review.md（4 个模板）

---

## 二期重审（同日下午，v2 迭代后状态）

用户要求"重新审查整个项目"。本轮读取了 v2 迭代后的全部文件（含新增的 review-orchestrator-protocol.md、index.md、v2 设计/计划文档），仍为**只分析未修改**。

### v2 已解决的 10 项旧缺陷
执行入口（SKILL.md 补 YAML frontmatter + 4 Phase 流程）、单窗口并行（新增 review-orchestrator-protocol 用 subagent 并行派发）、合并角色（新增 review-orchestrator 协议）、superpowers 双向交接（Superpowers Instructions 段）、CR- 统一编号、跨轮统计（index.md 的 Finding Tracking / Previous Round CR-ID）、日期前缀命名、v2 设计文档 12 缺陷映射 DR-001..009。

### 残留 / 新引入缺陷（重审核心发现）

**P0（内容损坏，真 bug）**：
1. `roles/test-designer.md` 第 885–971 行：字段描述（Verification Gap / Trigger Scenario 等）在代码块外重复出现 + 第 971 行多出悬挂 ``` 围栏——v2 重写时复制粘贴残留，导致文件结构损坏。

**P1（一致性，直接影响"标记采纳/不采纳 + 统计"诉求）**：
2. `templates/consolidated-review.md` 决策词汇冲突 4+ 套：
   - 第 199 行 Finding Status 用 7 值正确枚举
   - 第 774 行 Superpowers Instructions 决策选项正确
   - 第 868 行 Decision Records 骨架用 ACCEPT/REJECT/DEFER/MODIFY/DUPLICATE/SUPERSEDE/RESOLVE + Decision Status: COMPLETED
   - 第 852 行 Decision Queue 用 Decision Status: PENDING
   - 第 954–961 行 Review Statistics By Status 用 Open / Ready for Decision / Resolved / Represented Elsewhere
   → 同一概念 4 套命名，机器解析与人工标记全乱。
3. `CONFIRMED_GAP` 合同缺口：Test Designer（test-designer.md + test-review.md）用 `CONFIRMED_GAP` / `MATERIAL_RISK`，但 finding-protocol.md 的 Evidence Class 只列 `CONFIRMED_DEFECT / MATERIAL_RISK / DESIGN_PREFERENCE`，少了 CONFIRMED_GAP → 违反"统一 Finding 契约"。
4. P3 严重度死路：严重度枚举含 P3，但 decision-protocol 无对应决策分支（P3 要么被忽略要么强行归并）。
5. 关系分类矛盾：consolidation-protocol Section 6 定义 7 类（DUPLICATE/SAME_ROOT_CAUSE/RELATED/INDEPENDENT/CONTRADICTORY/SUBSET/CONSEQUENCE），finding-protocol 用 4 类 → 不一致。
6. 冲突状态不匹配：decision-protocol 声明取代 OPEN/READY_FOR_DECISION/RESOLVED/SUPERSEDED，但 templates/product-review.md、system-review.md 的 YAML status 仍写 "OPEN"，test-review.md 写 "PENDING_DECISION"。

**P2（完整性 / 陈旧）**：
7. `docs/codebase/.codebase-scan.txt` 陈旧（仍为旧 13 文件结构）。
8. `docs/项目一期建设审查报告.md` 陈旧（记录旧缺陷，已被 v2 修复）。
9. `.superpowers/sdd/progress.md` 陈旧（7 任务全 pending，实际文件已存在）。
10. `protocols/finding-protocol.md` 第 864–865 行多出悬挂 ``` 围栏。
11. `roles/system-critic.md` 第 948 行示例结尾有悬挂 ```id="0p8u3h" 残留。
12. **关键空档**：`docs/superpowers/reviews/` 下无任何实际跑过的审核产物——skill 从未端到端执行过，所有协议/template 仅为设计态，真实跑通后可能暴露更多问题。

### 重审结论
v2 解决了"工程化执行层"与"交接层"的空白，但对齐用户核心诉求（标记采纳/不采纳 + 统计）时，**内部一致性债反而加重**：同一决策概念 4 套命名、证据类缺口、严重度死路。建议优先修 P0（test-designer 内容损坏）与 P1-2（consolidated 决策词汇统一为 decision-protocol 的 7 态）。仍**未做任何项目文件修改**。

---

## 产出：项目二期审查报告

用户说"写报告"，据此将重审结论落盘为 `docs/项目二期审查报告.md`（此前仅分析未改文件，本报告为新增交付物，非修改 skill 内容）。

报告结构：背景痛点 → 一期 12 缺陷 v2 修复映射表（10 已解决/2 部分解决）→ v2 总体评估 → 缺陷明细 D-001~D-010（P0×1 / P1×5 / P2×4）→ 风险汇总 → 修复优先级表 → 结论。
报告前已用 grep + 精读复核全部指控：CONFIRMED_GAP 确实缺合同出处、consolidated 决策词汇 5 套、test-designer 885-971 重复、finding-protocol 864-865 悬挂围栏、system-critic 948 残留 ` ```id="0p8u3h" `、reviews 目录不存在。

**待办**：用户尚未授权修复任何缺陷。

---

## 三轮重审（23:02，用户再次调整后）

用户说"我又调整了一般，重新审查一下"。重新静态复核全部文件 + CommonMark 围栏校验（自写 python 脚本，严格按嵌套规则扫 17 个 md，仅 system-critic.md 不平衡）。

### 本轮新解决（用户修掉）
- D-002 consolidated-review.md 决策词汇 → 已统一为 7 态（PENDING_DECISION/ACCEPTED/REJECTED/DEFERRED/PARTIALLY_ACCEPTED/DUPLICATE/INVALIDATED），全篇一致；其余枚举（relationship_classification 7 类、conflict_status 4 值、disposition DUPLICATE|REPRESENTED_ELSEWHERE、processing_status）为不同轴，已确认各自内部一致。
- D-003 CONFIRMED_GAP 进入 finding-protocol.md 契约（L159 专节）。
- D-004 严重度无 P3，仅 P0/P1/P2（finding-protocol L311、CLAUDE.md、decision-protocol 一致）。
- D-005 关系分类 7 类成唯一真理（finding-protocol 不再定义冲突的 4 类；CLAUDE.md L67 + consolidated L1049 + consolidation L526 一致）。
- D-008 finding-protocol.md 末尾悬挂围栏已清，结尾正常（L866-867）。

### 部分解决 / 仍开
- D-009 system-critic.md：原 ` ```id="0p8u3h" ` 残留已删，但末行 980 又出现一个未闭合 ```（删 id 围栏时留下孤儿开围栏）→ 仍 P2 残。CommonMark 校验唯一不平衡项。
- D-006 role 模板（product/test 的 open_questions、system 的 irreversible_decisions/complexity_risks/open_questions）仍用 `status: "OPEN"`，与 decision-protocol L15-17 全局废弃 OPEN 冲突；但 Finding 本身已是 PENDING_DECISION → 降级 P2（建议给这些清单独立枚举或缩小废弃范围）。
- D-007 docs/codebase/ 已清空、.superpowers/sdd/progress.md 已空；但 `docs/项目一期建设审查报告.md` 仍陈旧（用户可保留为历史）。
- D-010 docs/superpowers/reviews/ 仍不存在，skill 从未端到端实跑。

### 新发现
- D-011 CLAUDE.md 证据等级摘要（L55-59）仍只列 3 类，漏 CONFIRMED_GAP（与 finding-protocol 现 4 类不一致）。
- D-012 finding-protocol.md L315 摘要枚举 "CONFIRMED_DEFECT / MATERIAL_RISK" 不全，漏 DESIGN_PREFERENCE 与 CONFIRMED_GAP。

### 本轮结论
相较二期，一致性债大幅消减：D-001~D-005、D-008 已清，D-002 修复使"标记+统计"的词汇基础成立。剩余 D-006/D-007/D-009/D-010 + 新 D-011/D-012 均为 P2 级（无内容损坏、不阻断流程）。最高杠杆收尾项：D-009（删 980 行一个 ```）、D-011/D-012（补 CLAUDE.md 与摘要枚举）、D-006（OPEN 语义澄清）。仍**未改任何项目文件**。

---

## 四轮验收（用户修复 D-006/009/010/011/012 后）

用户称已处理 D-009/D-011/D-012/D-006/D-010，要求验收。逐项核验：
- D-009 ✅ 正确：全部 14 个 md 文件 CommonMark 围栏校验通过，system-critic.md 末行孤儿开围栏已删。
- D-011 ✅ 正确：CLAUDE.md L57-60 证据等级补入 CONFIRMED_GAP（4 类，与契约一致）。
- D-012 ✅ 正确：finding-protocol.md 摘要枚举补齐为 CONFIRMED_DEFECT / MATERIAL_RISK / CONFIRMED_GAP / DESIGN_PREFERENCE。
- D-006 ✅ 正确（走"收窄废弃范围"路线）：decision-protocol.md L16-19 改为 "OPEN as a Finding decision state" 废弃，明确 "OPEN remains valid for non-Finding entities (Questions, Irreversible Decisions, Complexity Risks)"；模板残留的 5 处 status:"OPEN"（product-review open_questions、system-review irreversible_decisions/complexity_risks/open_questions、test-review open_questions）全部落在豁免范围，自洽。
- D-010 ❌ 未处理：docs/superpowers/reviews/ 仍不存在，skill 从未端到端实跑。此项需用真实 spec 跑一轮才算闭环。

结论：设计态缺陷已全清（D-001~D-009、D-011、D-012 全解决）。唯一剩项 D-010 是"实跑验证"，非静态可改。仍未改任何项目文件。
